<% @user.each do |user| %>
<p>
<%= user.id %>
<a href="users/<%= user.id %>">
    <%= user.nickname %>
</a>
    <% if Studysession.find_by(user:user.id,active:true) %>
      <font color="red">(勉強中)
      </font>
    <% end %>
</p>
<% end %>