<table class="table">
  <tbody>
    <% @user.each do |user| %>
      <tr>
        <td>
          <a href="users/<%= user.id %>">
              <%= user.nickname %>
          </a>
              <% if Studysession.find_by(user:user.id,active:true) %>
                <span class="badge badge-important">勉強中</span>
              <% end %>
            <%= user.word %>:
            <%= user.grade %>:
            <%= user.area %>
          </p>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>