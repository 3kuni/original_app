<% if signed_in? %>
  <% unless current_user.nickname.present? %>
    <div class="alert alert-danger" role="alert">
      <%= link_to "こちらから",edit_user_registration_path%>ニックネーム、学年、地域、ひとことを設定してください</div>
  <% end %>
<% end %>

<h1>STARDY</h1>
<h5>いま、誰が勉強してる？</h5>

<br>
<div>
<% unless signed_in? %>
  <%= link_to (fa_icon 'twitter')+ "  Sign in with Twitter",  user_omniauth_authorize_path(:twitter),class:"btn authtwi" %><br>
  <%= link_to (fa_icon 'facebook-square')+ "  Sign in with Facebook",  user_omniauth_authorize_path(:facebook),class:"btn authfb" %><br>
  <%= link_to (fa_icon 'envelope')+ "  新規登録",  new_user_registration_path,class:"btn register" %>
  <br>
<% end %>

<% unless @active_now.present? %>
    <div class="row" style="margin-top:3rem;">
      <div class="col  m12 l6">
        <div class="card-panel grey lighten-5">
          <span>
            <h5>1.部屋を選択</h5>
            目標や学年別に部屋があるので、好きな部屋に入って勉強することができます :)
            
          </span>
        </div>
      </div>
      <div class="col m12 l6">
        <div class="card-panel grey lighten-5">
          <span>
            <h5>2.教材・活動を選択</h5>
            Amazonで検索するか、教材を自分で登録すると勉強セッションが開始します！
          </span>
        </div>
      </div>
    </div>
  <%= link_to "今すぐ開始" , ssession_path ,class: "waves-effect waves-light btn-large start" %>
<% else %>
  <%= link_to "セッションに戻る", "/studysessions/studying/#{@active_now.id}/#{@active_now.room}" ,class: "waves-effect waves-light btn-large custombutton" %>
<% end %>

<br>


<!-- カード -->  
<% if signed_in? %>
    <h4>タイムライン</h4>
    <ul class="collection">
      <% @feed_items.each do |studysession| %>
        <li class="collection-item avatar 
          <% if studysession.active %>
            yellow lighten-5
          <% end %>
        ">
          <a href="http://amazon.co.jp/dp/<%= studysession.textbook %>" target="_blank">
              <img src="http://images-jp.amazon.com/images/P/<%= studysession.textbook %>.09.TZZZZZZZ.jpg" alt="" class="circle">
          </a>
          
          <span class="title">          
            <a href="/users/<%= studysession.user %>">
              <% @user=User.find(studysession.user) %>
              <%= @user.nickname %>
            </a>
          </span>
          <p>
            <% if studysession.active %>
              セッションを開始しました！
            <% else %>
              <%= studysession.time %>分のセッションを終了しました。
            <% end %>
            <br>
              <%= Room.find(studysession.room).name %>
          </p>
          
          <span class="secondary-content">
            
            
            <% if studysession.evaluators_for(:stars).include?(@current_user) %>
              <span style="color:gold;" class="star">
                <%= fa_icon 'star' %>
              </span>
            <% else %>
              <span style="color:lightgray" id="star-icon<%= studysession.id  %>" class="star">
                <%= link_to (fa_icon 'star'), "/studysessions/like/#{studysession.id}" , remote:true,:class=>"unstarred"%>
              </span>
            <% end %>
            <span id="star-sum<%= studysession.id  %>">
              <%= studysession.reputation_for(:stars).to_i %>
            </span>
          </span>
        </li>
      <% end %>
    </ul>

<% end %>
<!--ここまで -->  

