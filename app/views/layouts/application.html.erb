<!DOCTYPE html>
<html>
<head>
  <title>OriginalApp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <!-- <meta http-equiv="Refresh" content="60"> -->

</head>
<body style="padding-top:40px;">
<div class="container">
<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <a class="brand" href="/">STARDY</a>
    <ul class="nav pull-right">
      <% if user_signed_in? %>
        <li class="navbar-text"><strong><%= current_user.nickname %></strong>
          <% if @active_now.present? %>
            <span class="badge badge-important">勉強中</span>
          <% end %>
        </li>
        <li ><%= link_to (fa_icon 'bell'),"#" %></li>
        <li><%= link_to (fa_icon 'user') , users_path  %></li>
        <li><%= link_to (fa_icon 'cog'), edit_user_registration_path, :class => 'navbar-link' %></li>
      <% else %>
          <li><%= link_to "登録", new_user_registration_path, :class => 'navbar-link'  %></li> 
          <li><%= link_to "ログイン", new_user_session_path, :class => 'navbar-link'  %></li>
      <% end %>
    </ul>
  </div>
</div>

<% if notice %>
  <p class="alert alert-notice"><%= notice %></p>
<% end %>
<% if alert %>
  <p class="alert alert-error"><%= alert %></p>
<% end %>
<%= yield %>
<div>
      <%= debug(params) if Rails.env.development? %>
</body>
</html>
